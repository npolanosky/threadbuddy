{
  "name": "threadbuddy",
  "version": "0.1.0",
  "description": "ThreadBuddy — a cross-platform, open screw-thread dimension calculator for machining (clean-room reimplementation inspired by ME ThreadPal).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
